name: 'northwind'
version: '1.0.0'
profile: 'northwind'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  northwind:
    +database: workspace
    staging:
      +materialized: view
      +schema: silver
    marts:
      +materialized: table
      +schema: gold